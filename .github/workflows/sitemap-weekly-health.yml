# name: 🗺️ Sitemap Weekly Health Check

# on:
#   schedule:
#     - cron: '0 8 * * 1'
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# permissions:
#   contents: read
#   issues: write

# jobs:
#   validate-sitemap:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     outputs:
#       check_passed: ${{ steps.validate.outcome == 'success' }}
#     steps:
#       - name: 📥 Checkout Repository
#         uses: actions/checkout@v4

#       - name: 🧪 Validate sitemap.xml freshness + required URLs
#         id: validate
#         continue-on-error: true
#         run: |
#           node <<'EOF'
#           const fs = require('node:fs');

#           const sitemapPath = 'public/sitemap.xml';
#           const requiredUrls = [
#             'https://www.omerzahid.com/',
#             'https://www.omerzahid.com/about',
#             'https://www.omerzahid.com/ask-me',
#             'https://www.omerzahid.com/resume-generator',
#             'https://www.omerzahid.com/darthVader',
#           ];

#           if (!fs.existsSync(sitemapPath)) {
#             console.error(`Missing file: ${sitemapPath}`);
#             process.exit(1);
#           }

#           const xml = fs.readFileSync(sitemapPath, 'utf8');
#           const urlBlocks = [...xml.matchAll(/<url>([\s\S]*?)<\/url>/g)].map((m) => m[1]);

#           if (urlBlocks.length === 0) {
#             console.error('Sitemap has no <url> entries.');
#             process.exit(1);
#           }

#           const locs = [];
#           const issues = [];

#           for (const block of urlBlocks) {
#             const locMatch = block.match(/<loc>([^<]+)<\/loc>/);
#             const freqMatch = block.match(/<changefreq>([^<]+)<\/changefreq>/i);

#             if (!locMatch) {
#               issues.push('A <url> block is missing <loc>.');
#               continue;
#             }

#             const loc = locMatch[1].trim();
#             locs.push(loc);

#             if (!loc.startsWith('https://www.omerzahid.com/')) {
#               issues.push(`Non-canonical host found in sitemap URL: ${loc}`);
#             }

#             if (!freqMatch) {
#               issues.push(`Missing <changefreq> for URL: ${loc}`);
#             } else {
#               const freq = freqMatch[1].trim().toLowerCase();
#               if (freq !== 'weekly') {
#                 issues.push(`Expected weekly cadence for ${loc}, found: ${freq}`);
#               }
#             }
#           }

#           for (const requiredUrl of requiredUrls) {
#             if (!locs.includes(requiredUrl)) {
#               issues.push(`Missing required URL: ${requiredUrl}`);
#             }
#           }

#           if (issues.length > 0) {
#             console.error('Sitemap validation failed:');
#             for (const message of issues) console.error(`- ${message}`);
#             process.exit(1);
#           }

#           console.log(`Sitemap validation passed with ${locs.length} URL entries.`);
#           EOF

#   create-issue-on-failure:
#     needs: validate-sitemap
#     if: needs.validate-sitemap.outputs.check_passed != 'true'
#     runs-on: ubuntu-latest
#     steps:
#       - name: 🚨 Create issue for sitemap drift
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const owner = context.repo.owner;
#             const repo = context.repo.repo;
#             const labelName = 'seo-sitemap-health';
#             const now = new Date();
#             const datePart = `${now.getUTCFullYear()}-${String(now.getUTCMonth() + 1).padStart(2, '0')}-${String(now.getUTCDate()).padStart(2, '0')}`;
#             const title = `🗺️ Sitemap Health Check Failed - ${datePart}`;

#             try {
#               await github.rest.issues.getLabel({ owner, repo, name: labelName });
#             } catch (error) {
#               if (error.status === 404) {
#                 await github.rest.issues.createLabel({
#                   owner,
#                   repo,
#                   name: labelName,
#                   color: 'b60205',
#                   description: 'SEO sitemap health check failures'
#                 });
#               } else {
#                 throw error;
#               }
#             }

#             const existing = await github.rest.issues.listForRepo({
#               owner,
#               repo,
#               state: 'open',
#               labels: labelName,
#               per_page: 100
#             });

#             const duplicate = existing.data.find(issue => issue.title.trim() === title);
#             if (duplicate) {
#               core.info(`Issue already exists: #${duplicate.number}`);
#               return;
#             }

#             const runUrl = `https://github.com/${owner}/${repo}/actions/runs/${context.runId}`;
#             const body = [
#               '## Sitemap Weekly Health Check Failed',
#               '',
#               `**Run:** [View workflow run](${runUrl})`,
#               `**Date:** ${datePart}`,
#               '',
#               '### What failed',
#               '- Sitemap is missing required URLs and/or has non-weekly cadence entries.',
#               '- Or sitemap host URLs are not canonical (`https://www.omerzahid.com/`).',
#               '',
#               '### Required routes',
#               '- `https://www.omerzahid.com/`',
#               '- `https://www.omerzahid.com/about`',
#               '- `https://www.omerzahid.com/ask-me`',
#               '- `https://www.omerzahid.com/resume-generator`',
#               '- `https://www.omerzahid.com/darthVader`',
#               '',
#               '### Fix checklist',
#               '- [ ] Update `public/sitemap.xml` routes',
#               '- [ ] Ensure each route has `<changefreq>weekly</changefreq>`',
#               '- [ ] Ensure all loc values use `https://www.omerzahid.com/` host',
#               '- [ ] Re-run workflow to confirm green status',
#             ].join('\n');

#             const created = await github.rest.issues.create({
#               owner,
#               repo,
#               title,
#               body,
#               labels: [labelName]
#             });

#             core.info(`Created sitemap health issue #${created.data.number}`);

#   fail-on-invalid-sitemap:
#     needs:
#       - validate-sitemap
#       - create-issue-on-failure
#     if: always() && needs.validate-sitemap.outputs.check_passed != 'true'
#     runs-on: ubuntu-latest
#     steps:
#       - name: ❌ Mark workflow failed for sitemap drift
#         run: |
#           echo "Sitemap validation failed. See previous logs and created issue for details."
#           exit 1
