name: 🎮 Game Update Planner (Every 1 Week)

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  create-weekly-game-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Ensure tracking label exists
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const labelName = 'game-update-weekly';

            try {
              await github.rest.issues.getLabel({ owner, repo, name: labelName });
              core.info(`Label '${labelName}' already exists.`);
            } catch (error) {
              if (error.status === 404) {
                await github.rest.issues.createLabel({
                  owner,
                  repo,
                  name: labelName,
                  color: '1f6feb',
                  description: 'Recurring game updates every 1 week'
                });
                core.info(`Created label '${labelName}'.`);
              } else {
                throw error;
              }
            }

      - name: Create weekly game update issue (if missing)
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const now = new Date();

            const datePart = `${now.getUTCFullYear()}-${String(now.getUTCMonth() + 1).padStart(2, '0')}-${String(now.getUTCDate()).padStart(2, '0')}`;
            const title = `🎮 Weekly Game Update - ${datePart}`;

            const existing = await github.rest.issues.listForRepo({
              owner,
              repo,
              state: 'open',
              labels: 'game-update-weekly',
              per_page: 100
            });

            const duplicate = existing.data.find(issue => issue.title.trim() === title);
            if (duplicate) {
              core.info(`Issue already exists for ${datePart}: #${duplicate.number}`);
              return;
            }

            const body = [
              '## Weekly Game Update Checklist',
              '',
              'Source guidance: `src/features/game/agent.md`',
              '',
              '### Required work',
              '- [ ] Add one gameplay feature (mechanic/enemy/power-up/challenge)',
              '- [ ] Add one stability or performance improvement',
              '- [ ] Add one desktop/mobile UX polish item',
              '- [ ] Confirm no duplicate logic introduced',
              '- [ ] Keep business logic in `src/features/game/gameScripts` as needed',
              '',
              '### Validation',
              '- [ ] `npm.cmd run lint`',
              '- [ ] `npm.cmd run build`',
              '- [ ] Verify gameplay on desktop controls',
              '- [ ] Verify gameplay on mobile/touch controls',
              '',
              '### Notes',
              '- Keep changes focused and backward-compatible.',
              '- Link PRs and test notes here.'
            ].join('\n');

            const created = await github.rest.issues.create({
              owner,
              repo,
              title,
              body,
              labels: ['game-update-weekly']
            });

            core.info(`Created weekly game update issue #${created.data.number}`);
